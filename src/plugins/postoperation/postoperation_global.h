#pragma once

#include <qglobal.h>

#if defined(POSTOPERATION_LIBRARY)
#  define FEEM_POSTOPERATION_EXPORT Q_DECL_EXPORT
#elif defined(POSTOPERATION_STATIC_LIBRARY)
#  define FEEM_POSTOPERATION_EXPORT
#else
#  define FEEM_POSTOPERATION_EXPORT Q_DECL_IMPORT
#endif
